apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: orders
spec:
  selector:
    app: notification-service
  ports:
    - port: 80
      targetPort: 8085

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: orders
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: ghcr.io/sprasanna03/notification-service:latest
          ports:
            - containerPort: 8085
          env:
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: kafka.orders:9092
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: secret
