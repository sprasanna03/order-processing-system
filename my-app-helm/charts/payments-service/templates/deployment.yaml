apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "my-app-payments.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "my-app-payments.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "my-app-payments.name" . }}
    spec:
      containers:
        - name: payments
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8001
