apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "my-app-orders.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "my-app-orders.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "my-app-orders.name" . }}
    spec:
      containers:
        - name: orders
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8000
